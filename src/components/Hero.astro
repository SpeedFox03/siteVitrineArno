<section class="hero section" id="top">
  <div class="container hero-grid">
    <div class="hero-copy hero-reveal-left">
      <span class="eyebrow">Peinture haut de gamme · Résidentiel & professionnel</span>

      <h1>Des finitions premium pour des projets ambitieux.</h1>

      <p class="lead">
        <strong>Moment D. Art</strong> vous accompagne sur des chantiers de <strong>peinture</strong> et de <strong>finition</strong> avec une approche <strong>sobre</strong>, <strong>précise</strong> et <strong>haut de gamme</strong> :
        <strong>peinture intérieure</strong>, <strong>peinture extérieure</strong>, <strong>peinture industrielle et époxy</strong>,
        <strong>revêtements de sol</strong> et <strong>muraux</strong>, <strong>boiseries</strong> et <strong>ferronneries</strong>.
      </p>

      <div class="btn-row hero-actions">
        <a class="btn btn-primary" href={`${import.meta.env.BASE_URL}realisations`}>Voir les réalisations</a>
        <a class="btn btn-secondary" href={`${import.meta.env.BASE_URL}services`}>Découvrir les services</a>
      </div>
    </div>

    <div class="hero-media hero-reveal-right">
      <div class="video-frame">
        <video autoplay muted loop playsinline>
          <source src={`${import.meta.env.BASE_URL}videos/intro.mp4`} type="video/mp4" />
        </video>
      </div>
    </div>
  </div>
</section>

<style>
  .hero-grid {
    display: grid;
    grid-template-columns: 1.02fr 0.98fr;
    grid-template-areas: "copy media";
    gap: 1.5rem;
    align-items: center;
  }

  .hero-copy {
    grid-area: copy;
    display: flex;
    flex-direction: column;
    gap: 1.5rem;
    justify-content: center;
  }

  .hero-media {
    grid-area: media;
  }

  .eyebrow {
    display: inline-flex;
    align-items: center;
    gap: 0.6rem;
    letter-spacing: 0.14em;
    text-transform: uppercase;
    font-size: 0.74rem;
    font-weight: 700;
    color: #7A5C3E;
  }

  .eyebrow::before {
    content: '';
    width: 28px;
    height: 1px;
    background: rgba(122, 92, 62, 0.42);
  }

  .video-frame {
    position: relative;
    width: 100%;
    aspect-ratio: 16 / 9;
    border-radius: 26px;
    overflow: hidden;
    background: #d7cec1;
    border: 1px solid rgba(23, 22, 20, 0.06);
    box-shadow: 0 18px 40px rgba(23, 22, 20, 0.08);
  }

  .video-frame::after {
    content: '';
    position: absolute;
    inset: 0;
    background:
      linear-gradient(180deg, rgba(16,12,8,0.04), transparent 28%, rgba(16,12,8,0.22)),
      linear-gradient(135deg, rgba(255,255,255,0.08), transparent 40%);
    pointer-events: none;
  }

  .video-frame video {
    width: 100%;
    height: 100%;
    display: block;
    object-fit: cover;
    object-position: center;
  }

  .hero-reveal-left,
  .hero-reveal-right {
    opacity: 0;
    animation-duration: 1s;
    animation-timing-function: cubic-bezier(0.22, 1, 0.36, 1);
    animation-fill-mode: forwards;
  }

  .hero-reveal-left {
    transform: translateX(-32px);
    animation-name: heroRevealLeft;
    animation-delay: 0.12s;
  }

  .hero-reveal-right {
    transform: translateX(32px);
    animation-name: heroRevealRight;
    animation-delay: 0.24s;
  }

  @keyframes heroRevealLeft {
    from { opacity: 0; transform: translateX(-32px); }
    to { opacity: 1; transform: translateX(0); }
  }

  @keyframes heroRevealRight {
    from { opacity: 0; transform: translateX(32px); }
    to { opacity: 1; transform: translateX(0); }
  }

  @media (prefers-reduced-motion: reduce) {
    .hero-reveal-left,
    .hero-reveal-right {
      opacity: 1;
      transform: none;
      animation: none;
    }
  }

  @media (max-width: 900px) {
    .hero-grid {
      grid-template-columns: 1fr;
      grid-template-areas:
        "copy"
        "media";
      gap: 1.25rem;
    }

    .hero-copy {
      gap: 1.2rem;
    }

    .video-frame {
      border-radius: 22px;
    }
  }

  @media (max-width: 720px) {
    .hero {
      padding-top: 2.4rem;
    }

    .hero-copy {
      gap: 1rem;
    }

    .hero-copy h1 {
      max-width: 11ch;
    }

    .hero-copy .lead {
      font-size: 1rem;
      line-height: 1.68;
      max-width: 100%;
    }

    .hero-actions {
      display: grid;
      grid-template-columns: 1fr;
      gap: 0.75rem;
      width: 100%;
    }

    .hero-actions .btn {
      width: 100%;
    }


  @media (max-width: 480px) {
    .hero {
      padding-top: 2rem;
    }

    .hero-grid {
      gap: 1rem;
    }

    .eyebrow {
      font-size: 0.68rem;
      letter-spacing: 0.12em;
    }

    .eyebrow::before {
      width: 22px;
    }

    .hero-copy .lead {
      font-size: 0.98rem;
    }

    .video-frame {
      border-radius: 18px;
    }
  }
</style>