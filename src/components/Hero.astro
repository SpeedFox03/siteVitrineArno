<section class="hero section" id="top">
  <div class="container hero-grid">
    <div class="hero-copy hero-reveal-left">
      <span class="eyebrow">Peinture haut de gamme · Résidentiel & professionnel</span>

      <h1>Des finitions premium pour des projets ambitieux.</h1>

      <p class="lead">
        <strong>Moment D. Art</strong> vous accompagne sur des chantiers de <strong>peinture</strong> et de <strong>finition</strong> avec une approche <strong>sobre</strong>, <strong>précise</strong> et <strong>haut de gamme</strong> :
        <strong>peinture intérieure</strong>, <strong>peinture extérieure</strong>, <strong>enduits décoratifs</strong>,
        <strong>revêtements de sol</strong> et <strong>muraux</strong>, <strong>boiseries</strong> et <strong>ferronneries</strong>.
      </p>
    </div>

    <div class="hero-cta hero-reveal-right">
      <a class="cta-card cta-primary" href={`${import.meta.env.BASE_URL}devis`}>
        <span class="cta-icon">
          <svg width="28" height="28" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.7" stroke-linecap="round" stroke-linejoin="round">
            <path d="M14 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V8z"/>
            <polyline points="14 2 14 8 20 8"/>
            <line x1="16" y1="13" x2="8" y2="13"/>
            <line x1="16" y1="17" x2="8" y2="17"/>
            <polyline points="10 9 9 9 8 9"/>
          </svg>
        </span>
        <span class="cta-body">
          <strong>Demander un devis</strong>
          <span>Chiffrage personnalisé, avec projection visuelle du résultat avant travaux.</span>
        </span>
        <span class="cta-arrow" aria-hidden="true">
          <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
            <line x1="5" y1="12" x2="19" y2="12"/>
            <polyline points="12 5 19 12 12 19"/>
          </svg>
        </span>
      </a>

      <a class="cta-card cta-secondary" href={`${import.meta.env.BASE_URL}#contact`}>
        <span class="cta-icon">
          <svg width="28" height="28" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.7" stroke-linecap="round" stroke-linejoin="round">
            <path d="M21 15a2 2 0 0 1-2 2H7l-4 4V5a2 2 0 0 1 2-2h14a2 2 0 0 1 2 2z"/>
          </svg>
        </span>
        <span class="cta-body">
          <strong>Prendre contact</strong>
          <span>Une question, une demande rapide ? On vous répond dans les meilleurs délais.</span>
        </span>
        <span class="cta-arrow" aria-hidden="true">
          <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
            <line x1="5" y1="12" x2="19" y2="12"/>
            <polyline points="12 5 19 12 12 19"/>
          </svg>
        </span>
      </a>
    </div>
  </div>
</section>

<style>
  .hero-grid {
    display: grid;
    grid-template-columns: 1.1fr 0.9fr;
    gap: 2.5rem;
    align-items: center;
  }

  .hero-copy {
    display: flex;
    flex-direction: column;
    gap: 1.5rem;
    justify-content: center;
  }

  .hero-cta {
    display: flex;
    flex-direction: column;
    gap: 1rem;
  }

  .eyebrow {
    display: inline-flex;
    align-items: center;
    gap: 0.6rem;
    letter-spacing: 0.14em;
    text-transform: uppercase;
    font-size: 0.74rem;
    font-weight: 700;
    color: #7A5C3E;
  }

  .eyebrow::before {
    content: '';
    width: 28px;
    height: 1px;
    background: rgba(122, 92, 62, 0.42);
  }

  /* CTA cards */
  .cta-card {
    display: flex;
    align-items: center;
    gap: 1.2rem;
    padding: 1.6rem 1.5rem;
    border-radius: 22px;
    text-decoration: none;
    border: 1px solid transparent;
    transition:
      transform 0.25s cubic-bezier(0.22, 1, 0.36, 1),
      box-shadow 0.25s cubic-bezier(0.22, 1, 0.36, 1),
      background 0.2s ease,
      border-color 0.2s ease;
  }

  .cta-card:hover {
    transform: translateY(-3px);
  }

  .cta-primary {
    background: #7A5C3E;
    color: #fff;
    box-shadow: 0 8px 28px rgba(122, 92, 62, 0.28);
  }

  .cta-primary:hover {
    background: #6b4f35;
    box-shadow: 0 14px 36px rgba(122, 92, 62, 0.38);
  }

  .cta-secondary {
    background: rgba(255, 255, 255, 0.72);
    color: var(--text);
    border-color: rgba(36, 29, 23, 0.10);
    box-shadow: 0 4px 18px rgba(23, 22, 20, 0.06);
    backdrop-filter: blur(8px);
  }

  .cta-secondary:hover {
    background: rgba(255, 255, 255, 0.92);
    border-color: rgba(122, 92, 62, 0.22);
    box-shadow: 0 10px 28px rgba(23, 22, 20, 0.10);
  }

  .cta-icon {
    flex-shrink: 0;
    display: flex;
    align-items: center;
    justify-content: center;
    width: 52px;
    height: 52px;
    border-radius: 14px;
  }

  .cta-primary .cta-icon {
    background: rgba(255, 255, 255, 0.15);
    color: #fff;
  }

  .cta-secondary .cta-icon {
    background: rgba(122, 92, 62, 0.10);
    color: #7A5C3E;
  }

  .cta-body {
    flex: 1;
    display: flex;
    flex-direction: column;
    gap: 0.3rem;
  }

  .cta-body strong {
    font-size: 1.08rem;
    font-weight: 700;
    display: block;
  }

  .cta-body span {
    font-size: 0.86rem;
    line-height: 1.5;
    opacity: 0.82;
  }

  .cta-primary .cta-body span {
    opacity: 0.78;
  }

  .cta-arrow {
    flex-shrink: 0;
    display: flex;
    align-items: center;
    opacity: 0.6;
    transition: transform 0.2s ease, opacity 0.2s ease;
  }

  .cta-card:hover .cta-arrow {
    transform: translateX(4px);
    opacity: 1;
  }

  /* Animations */
  .hero-reveal-left,
  .hero-reveal-right {
    opacity: 0;
    animation-duration: 1s;
    animation-timing-function: cubic-bezier(0.22, 1, 0.36, 1);
    animation-fill-mode: forwards;
  }

  .hero-reveal-left {
    transform: translateX(-32px);
    animation-name: heroRevealLeft;
    animation-delay: 0.12s;
  }

  .hero-reveal-right {
    transform: translateX(32px);
    animation-name: heroRevealRight;
    animation-delay: 0.28s;
  }

  @keyframes heroRevealLeft {
    from { opacity: 0; transform: translateX(-32px); }
    to   { opacity: 1; transform: translateX(0); }
  }

  @keyframes heroRevealRight {
    from { opacity: 0; transform: translateX(32px); }
    to   { opacity: 1; transform: translateX(0); }
  }

  @media (prefers-reduced-motion: reduce) {
    .hero-reveal-left,
    .hero-reveal-right {
      opacity: 1;
      transform: none;
      animation: none;
    }
  }

  @media (max-width: 900px) {
    .hero-grid {
      grid-template-columns: 1fr;
      gap: 1.5rem;
    }

    .hero-cta {
      flex-direction: row;
    }

    .cta-card {
      flex: 1;
    }
  }

  @media (max-width: 640px) {
    .hero-cta {
      flex-direction: column;
    }

    .cta-card {
      padding: 1.3rem 1.2rem;
    }

    .cta-icon {
      width: 44px;
      height: 44px;
    }
  }

  @media (max-width: 480px) {
    .eyebrow {
      font-size: 0.68rem;
      letter-spacing: 0.12em;
    }

    .eyebrow::before {
      width: 22px;
    }

    .cta-body strong {
      font-size: 1rem;
    }
  }
</style>
