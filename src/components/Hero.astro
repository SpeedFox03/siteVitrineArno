<section class="hero section" id="top">
  <div class="container hero-grid">
    <div class="hero-copy hero-reveal-left">
      <span class="eyebrow">Peinture haut de gamme · Résidentiel & professionnel</span>
      <h1>Des finitions premium pour des projets ambitieux.</h1>
      <p class="lead">
        Arno Momin vous accompagne sur des chantiers de peinture et de finition avec une approche sobre, précise et haut de gamme :
        peinture intérieure, extérieure, boiseries, ascenseurs, revêtements muraux et pose de parquet flottant.
      </p>

      <div class="btn-row">
        <a class="btn btn-primary" href="/realisations">Voir les réalisations</a>
        <a class="btn btn-secondary" href="/services/peinture-interieure">Découvrir les services</a>
      </div>

      <div class="hero-metrics">
        <article class="hero-reveal-up delay-1 metric-card">
          <span class="number">01</span>
          <p>Interventions sur appartements, maisons et projets professionnels avec un niveau de finition exigeant.</p>
        </article>

        <article class="hero-reveal-up delay-2 metric-card">
          <span class="number">02</span>
          <p>Accompagnement complet : conseils, préparation, exécution et rendu final maîtrisé.</p>
        </article>
      </div>
    </div>

    <div class="hero-panel card hero-reveal-right">
      <div class="video-frame">
        <video autoplay muted loop playsinline poster="https://images.unsplash.com/photo-1505693416388-ac5ce068fe85?auto=format&fit=crop&w=1600&q=80">
          <source src="/videos/intro.mp4" type="video/mp4" />
        </video>
      </div>

      <div class="identity-row">
        <div class="brand-mark">
          <img src="/images/logo-premium-transparent.png" alt="Logo Moment D.Art" />
        </div>
        <div class="identity-copy">
          <span>Présentation</span>
          <p>Arno Momin · Rue des Moges · 4120 Neupré. Peintre depuis <strong>X années</strong>, il propose un travail de qualité au meilleur prix, avec une vraie exigence de finition.</p>
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  .hero-grid {
    display: grid;
    grid-template-columns: 1.02fr 0.98fr;
    gap: 1.5rem;
    align-items: center;
    min-height: calc(100vh - 7rem);
  }

  .hero-copy {
    display: flex;
    flex-direction: column;
    gap: 1.5rem;
  }

  .eyebrow {
    display: inline-flex;
    align-items: center;
    gap: 0.6rem;
    letter-spacing: 0.14em;
    text-transform: uppercase;
    font-size: 0.74rem;
    font-weight: 700;
    color: #7A5C3E;
  }

  .eyebrow::before {
    content: '';
    width: 28px;
    height: 1px;
    background: rgba(122, 92, 62, 0.42);
  }

  .hero-metrics {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 1rem;
    margin-top: 0.8rem;
    padding-top: 1.4rem;
    border-top: 1px solid var(--line);
  }

  .hero-metrics article {
    display: grid;
    gap: 0.65rem;
  }

  .metric-card {
    padding: 1rem 1rem 0.95rem;
    border-radius: 20px;
    background: linear-gradient(180deg, rgba(255,255,255,0.72), rgba(247,243,236,0.4));
    border: 1px solid rgba(23,22,20,0.06);
  }

  .hero-panel {
    padding: 1rem;
    display: grid;
    gap: 1rem;
    position: relative;
    overflow: hidden;
  }

  .hero-panel::before {
    content: '';
    position: absolute;
    inset: 0;
    background: linear-gradient(135deg, rgba(255,255,255,0.22), rgba(255,255,255,0.03) 40%, transparent 65%);
    opacity: 0.65;
    pointer-events: none;
  }

  .video-frame {
    position: relative;
    border-radius: 24px;
    overflow: hidden;
    aspect-ratio: 16 / 10;
    background: #d7cec1;
  }

  .video-frame::after {
    content: '';
    position: absolute;
    inset: 0;
    background:
      linear-gradient(180deg, rgba(16,12,8,0.04), transparent 28%, rgba(16,12,8,0.28)),
      linear-gradient(135deg, rgba(255,255,255,0.08), transparent 40%);
    pointer-events: none;
  }

  .video-frame video {
    width: 100%;
    height: 100%;
    object-fit: cover;
  }

  .identity-row {
    display: grid;
    grid-template-columns: 0.9fr 1.1fr;
    gap: 1rem;
    align-items: center;
    padding: 0.5rem;
    position: relative;
    z-index: 1;
  }

  .brand-mark {
    background: #f0ebe4;
    border: 1px solid rgba(36,29,23,0.08);
    border-radius: 22px;
    padding: 1.2rem;
  }

  .brand-mark img {
    width: 100%;
    object-fit: contain;
  }

  .identity-copy span {
    display: inline-block;
    margin-bottom: 0.55rem;
    text-transform: uppercase;
    letter-spacing: 0.12em;
    font-size: 0.75rem;
    font-weight: 700;
    color: #7A5C3E;
  }

  .hero-reveal-left,
  .hero-reveal-right,
  .hero-reveal-up {
    opacity: 0;
    animation-duration: 1s;
    animation-timing-function: cubic-bezier(0.22, 1, 0.36, 1);
    animation-fill-mode: forwards;
  }

  .hero-reveal-left {
    transform: translateX(-32px);
    animation-name: heroRevealLeft;
    animation-delay: 0.12s;
  }

  .hero-reveal-right {
    transform: translateX(32px);
    animation-name: heroRevealRight;
    animation-delay: 0.24s;
  }

  .hero-reveal-up {
    transform: translateY(28px);
    animation-name: heroRevealUp;
  }

  .delay-1 { animation-delay: 0.55s; }
  .delay-2 { animation-delay: 0.72s; }

  @keyframes heroRevealLeft {
    from { opacity: 0; transform: translateX(-32px); }
    to { opacity: 1; transform: translateX(0); }
  }

  @keyframes heroRevealRight {
    from { opacity: 0; transform: translateX(32px); }
    to { opacity: 1; transform: translateX(0); }
  }

  @keyframes heroRevealUp {
    from { opacity: 0; transform: translateY(28px); }
    to { opacity: 1; transform: translateY(0); }
  }

  @media (prefers-reduced-motion: reduce) {
    .hero-reveal-left,
    .hero-reveal-right,
    .hero-reveal-up {
      opacity: 1;
      transform: none;
      animation: none;
    }
  }

  @media (max-width: 980px) {
    .hero-grid {
      grid-template-columns: 1fr;
      min-height: auto;
      padding-top: 1.2rem;
    }
  }

  @media (max-width: 720px) {
    .hero-metrics,
    .identity-row {
      grid-template-columns: 1fr;
    }
  }
</style>