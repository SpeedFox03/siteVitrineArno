<section class="hero section" id="top">
  <div class="container hero-grid">
    <div class="hero-copy hero-reveal-left">
      <span class="eyebrow">Peinture haut de gamme · Résidentiel & professionnel</span>

      <h1>Peintre et enduits décoratifs haut de gamme à Neupré</h1>

      <p class="lead">
        <strong>Moment D. Art</strong> vous accompagne sur vos chantiers de <strong>peinture</strong> et <strong>finition</strong> avec une approche sobre, précise et haut de gamme. Basés à Neupré, nous intervenons dans toute la région liégeoise — et également à Namur, Ciney, Dinant et dans le Condroz.
      </p>

      <div class="hero-buttons">
        <a class="btn btn-primary" href={`${import.meta.env.BASE_URL}devis`}>
          Demander un devis
        </a>
        <a class="btn btn-secondary" href={`${import.meta.env.BASE_URL}#contact`}>
          Prendre contact
        </a>
      </div>
    </div>

    <div class="hero-image hero-reveal-right" aria-hidden="true">
      <img
        src={`${import.meta.env.BASE_URL}images/a-propos/img-momentdart.jpg`}
        alt="Arno Momin, peintre à Neupré et en région liégeoise — fondateur de Moment D.Art"
        width="1448"
        height="1086"
        loading="eager"
        decoding="async"
      />
    </div>
  </div>
</section>

<style>
  .hero-grid {
    display: grid;
    grid-template-columns: 1fr 0.75fr;
    gap: 3rem;
    align-items: center;
  }

  .hero-copy {
    display: flex;
    flex-direction: column;
    gap: 1.5rem;
    justify-content: center;
  }

  .eyebrow {
    display: inline-flex;
    align-items: center;
    gap: 0.6rem;
    letter-spacing: 0.14em;
    text-transform: uppercase;
    font-size: 0.74rem;
    font-weight: 700;
    color: #7A5C3E;
  }

  .eyebrow::before {
    content: '';
    width: 28px;
    height: 1px;
    background: rgba(122, 92, 62, 0.42);
  }

  /* Buttons */
  .hero-buttons {
    display: flex;
    gap: 0.75rem;
    flex-wrap: wrap;
  }

  .btn {
    display: inline-flex;
    align-items: center;
    padding: 0.6rem 1.4rem;
    border-radius: 999px;
    font-size: 0.88rem;
    font-weight: 600;
    text-decoration: none;
    letter-spacing: 0.01em;
    transition:
      transform 0.2s cubic-bezier(0.22, 1, 0.36, 1),
      box-shadow 0.2s ease,
      background 0.15s ease,
      border-color 0.15s ease;
  }

  .btn:hover {
    transform: translateY(-2px);
  }

  .btn-primary {
    background: #7A5C3E;
    color: #fff;
    box-shadow: 0 4px 16px rgba(122, 92, 62, 0.30);
  }

  .btn-primary:hover {
    background: #6b4f35;
    box-shadow: 0 8px 24px rgba(122, 92, 62, 0.38);
  }

  .btn-secondary {
    background: transparent;
    color: #7A5C3E;
    border: 1.5px solid rgba(122, 92, 62, 0.40);
  }

  .btn-secondary:hover {
    border-color: #7A5C3E;
    background: rgba(122, 92, 62, 0.06);
  }

  /* Hero image */
  .hero-image {
    border-radius: 20px;
    overflow: hidden;
    box-shadow: 0 20px 60px rgba(23, 22, 20, 0.14);
  }

  .hero-image img {
    width: 100%;
    height: auto;
    display: block;
  }

  /* Animations */
  .hero-reveal-left,
  .hero-reveal-right {
    opacity: 0;
    animation-duration: 1s;
    animation-timing-function: cubic-bezier(0.22, 1, 0.36, 1);
    animation-fill-mode: forwards;
  }

  .hero-reveal-left {
    transform: translateX(-32px);
    animation-name: heroRevealLeft;
    animation-delay: 0.12s;
  }

  .hero-reveal-right {
    transform: translateX(32px);
    animation-name: heroRevealRight;
    animation-delay: 0.28s;
  }

  @keyframes heroRevealLeft {
    from { opacity: 0; transform: translateX(-32px); }
    to   { opacity: 1; transform: translateX(0); }
  }

  @keyframes heroRevealRight {
    from { opacity: 0; transform: translateX(32px); }
    to   { opacity: 1; transform: translateX(0); }
  }

  @media (prefers-reduced-motion: reduce) {
    .hero-reveal-left,
    .hero-reveal-right {
      opacity: 1;
      transform: none;
      animation: none;
    }
  }

  /* Tablette */
  @media (max-width: 900px) {
    .hero-grid {
      grid-template-columns: 1fr;
      gap: 2rem;
    }

    .hero-image {
      max-height: 340px;
      aspect-ratio: 16 / 9;
    }
  }

  /* Mobile — photo cachée */
  @media (max-width: 640px) {
    .hero-image {
      display: none;
    }

    .hero-buttons {
      flex-direction: column;
    }

    .btn {
      justify-content: center;
    }
  }
</style>
