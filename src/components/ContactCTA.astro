<section class="section" id="contact">
  <div class="container">
    <div class="cta card">
      <div class="cta-copy">
        <span class="eyebrow">Contact</span>
        <h2>Parlons de votre projet.</h2>
        <p class="lead">
          Le site est prêt à accueillir vos vraies réalisations, vos zones d’intervention et un formulaire de demande de devis adapté à des projets premium.
        </p>
      </div>
      <div class="cta-actions">
        <a class="btn btn-primary" href="mailto:contact@momentdart.fr">contact@momentdart.fr</a>
        <a class="btn btn-secondary" href="tel:+33000000000">Téléphone</a>
      </div>
    </div>
  </div>
</section>

<style>
  .cta {
    padding: 1.8rem;
    display: grid;
    grid-template-columns: 1.15fr auto;
    gap: 1rem;
    align-items: center;
    background: linear-gradient(135deg, rgba(255,255,255,0.9), rgba(240,233,223,0.86));
  }
  .cta-actions {
    display: flex;
    flex-wrap: wrap;
    gap: 0.8rem;
    justify-content: flex-end;
  }
  @media (max-width: 900px) {
    .cta { grid-template-columns: 1fr; }
    .cta-actions { justify-content: flex-start; }
  }
</style>
